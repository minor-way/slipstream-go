# Slipstream-Go Environment Configuration
# Copy this file to .env and modify as needed

# Tunnel domain(s) - for multi-domain, configure in docker-compose.yml
DOMAIN=tunnel.example.com

# Server DNS port (UDP)
DNS_PORT=5353

# Client SOCKS5 port
SOCKS_PORT=1080

# Target routing: direct or socks5
TARGET_TYPE=direct

# Upstream SOCKS5 address (only if TARGET_TYPE=socks5)
TARGET=127.0.0.1:7020

# DNS resolver address for client
RESOLVER=your-server-ip:5353

# Logging level: debug, info, warn, error
LOG_LEVEL=info

# Server memory limit in MB
MEMORY_LIMIT=400

# Timezone
TZ=UTC
